# Static-site release audits

For a static-site release validation request, use the `static-site-release-audit` Skill from `.agents/skills/static-site-release-audit/`. Apply this repository’s release policy for routes, canonical URLs, and human visual review. Do not deploy, submit forms, or change release configuration without explicit approval.
